# TODO:
- 3d View and table should extend to page borders, not be horizontaly limited
- Dragging to rotate in the 3d view and releaseing when the pointer is on an object marks the object as selected, even if the pointer is not over the object when the mouse button is released. This should be fixed so that the object is only selected if the pointer is over it when the mouse button is released.
